package templ

import "github.com/ViBiOh/strava/pkg/model"

templ Result(uri, nonce, title, staticImageURL string, commutes model.Commutes) {
  @Root(uri, nonce, title) {
    <img src={ staticImageURL } alt="Static image of home and work"/>
    <pre>
      { commutes.String() }
    </pre>
  }
}
